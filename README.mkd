# Página Academia

Projeto desenvolvido para cadastro de participantes em uma competição esportiva utilizando HTML, CSS, PHP e MySQL.

## Funcionalidades

- Cadastro de participantes
- Armazenamento dos dados em banco de dados MySQL
- Página de conclusão após o cadastro
- Geração de comprovante em PDF utilizando FPDF
- Layout responsivo com HTML e CSS

## Estrutura do Projeto

```text
PAGINAACADEMIA/

├── guiabancodedados/
│   └── formulariosite.sql

├── fpdf19/

├── img/
│   ├── Basquete.jpg
│   ├── Futebol.jpg
│   ├── Futsal.jpg
│   ├── Handebol.jpg
│   ├── Volei.jpg
│   └── img_rodape.jpg

├── cabecalho_pg3.html
├── carrossel.html
├── comprovante.php
├── conexao.php
├── conteudo_pg3.php
├── formulario.html
├── paginaPrin.html
├── pg-Conclusao.php
├── rodape_pg3.html
├── salvar.php
└── style.css
```

## Tecnologias Utilizadas

- HTML5
- CSS3
- PHP
- MySQL
- XAMPP
- FPDF

## Requisitos

- XAMPP
- PHP 8 ou superior
- MySQL
- Navegador Web

## Como Baixar o Projeto

Para baixar uma cópia deste projeto para o seu computador, siga os passos abaixo:

### 1. Abra o terminal

No Windows, você pode utilizar o **Git Bash**, **Prompt de Comando (CMD)** ou **PowerShell**.

### 2. Navegue até a pasta onde deseja salvar o projeto

Exemplo:

```bash
cd Documentos/Projetos
```

### 3. Clone o repositório

Execute o comando abaixo:

```bash
git clone https://github.com/kafu14/PaginaAcademia.git
```

O comando `git clone` cria uma cópia completa do repositório em sua máquina local.

### 4. Acesse a pasta do projeto

```bash
cd PaginaAcademia
```

### 5. Abra o projeto

Agora basta abrir a pasta do projeto em sua IDE ou editor de código preferido, como:

* Visual Studio Code
* Visual Studio
* Sublime Text
* IntelliJ IDEA

### 6. Executar o projeto

Caso seja um projeto apenas com HTML, CSS e JavaScript, abra o arquivo `index.html` no navegador ou utilize a extensão **Live Server** do VS Code para uma melhor experiência.

## Atualizando o projeto

Se você já possui o projeto baixado e deseja obter as alterações mais recentes do repositório, execute:

```bash
git pull
```

Esse comando sincroniza sua cópia local com a versão mais recente disponível no GitHub.


## Configuração do Banco de Dados

1. Inicie o Apache e o MySQL pelo XAMPP.
  -configure para a porta 3007.

2. Acesse o phpMyAdmin:

```text
http://localhost/phpmyadmin
```

3. Clique na opção **Importar**.

4. Selecione o arquivo:

```text
PaginaAcademia/guiabancodedados/formulariosite.sql
```

5. Clique em **Executar**.

O banco de dados `formulariosite` e todas as tabelas serão criados automaticamente.

Caso necessário, ajuste as configurações de conexão no arquivo `conexao.php`.

## Como Executar

1. Copie a pasta `PaginaAcademia` para:

```text
C:\xampp\htdocs\
```

2. Inicie o Apache e o MySQL pelo XAMPP.

3. Acesse no navegador:

```text
http://localhost/PaginaAcademia/paginaPrin.html
```

## Banco de Dados

Tabela principal:

- inscricao_participante

Campos:

- nome
- email
- telefone
- nascimento
- genero
- rua
- cidade
- estado
- cep
- altura
- peso
- atividade

## Autor

Elionilson Viana